services:
  cloudkey:
    image: ghcr.io/ezzzi-y/cloud-key:latest
    container_name: cloudkey
    ports:
      - "9696:8080"
    environment:
      - CONFIG_PATH=/app/config.yaml
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs
    restart: unless-stopped
